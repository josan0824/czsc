---
name: chan-stock-buy-sell-point
description: 基于缠论技术分析和日线 OHLCV 行情数据，判断一只股票当前是否处于缠论一类、二类、三类买点或卖点，并生成详细 HTML 报告。主要使用网络获取数据来源，可分析 A 股、港股等。用户只要要求判断"股票是否是一二三类买卖点""缠论买卖点""一买/二买/三买/一卖/二卖/三卖""按缠中说禅分析某股票当前买卖点"，即使没有明确说使用本 skill，也应使用本 skill。输入通常是股票名称或代码，输出必须包含详细证据、反证、级别说明、结论置信度和 HTML 文件路径。
agent_created: true
---

# 缠论股票一二三类买卖点分析

当用户询问某只股票当前是否处于缠论一类、二类、三类买点或卖点时，使用本 skill。输入通常为股票名称或代码；输出要求为详细的缠论中文分析，并附带一份交互式 HTML 报告。

本 skill 为分析辅助工具，不构成投资建议。需明确说明数据局限性和分析的不确定性。在没有完整多级别证据链的情况下，切勿将买卖点判定为"确认"。

## 数据来源
- **web（默认且唯一推荐）**：通过网络获取行情，无需 API Key，支持 A 股、港股等市场。
- 网络取数必须显示实际成功渠道，例如 `AKShare-新浪财经 (000001.SZ)`、`新浪分钟K线 (sz000001, 5分钟, 5001根)`、`东方财富 (399852, 30分钟, 1488根)`，不得只写笼统的"网络数据"。
- 当前脚本已接入的稳定日线渠道包括：A 股 `AKShare-新浪财经`、`东方财富`，A 股指数 `AKShare-新浪财经指数`、`东方财富`，港股 `AKShare-港股`、`东方财富`，中证指数 `AKShare-中证指数`、`东方财富`。
- 分钟线取数策略：对同一级别同时尝试 `新浪分钟K线`、`东方财富`、可选 `mootdx`、可选 `baostock`，1 分钟还会额外尝试 `东方财富分时`（通常可取最近 5 个交易日）和 `通达信1分钟`（默认取最近 30 个自然日），每个来源都请求该接口可返回的最大近期 K 线，最终选择返回根数最多且可解析的来源；若根数相同，按候选顺序选择。未选中的来源需在 JSON 的 `timeframes[].source_candidates` 中记录返回根数或失败原因。
- 常规分析不得依赖本地 `Stock/` CSV 缓存，避免读到陈旧数据。只有用户明确要求调试本地数据时，才可显式使用 `--source local`。

## 股票解析

如果用户提供的是股票中文名称（如"腾讯控股"），系统会自动通过网络接口搜索名称并解析为代码。若网络搜索无法唯一匹配，再询问用户提供股票代码。

股票代码支持以下格式：
- A 股：`600519.SH`、`000001.SZ`、`688001.SH` 或纯数字 `600519`
- A 股指数：`上证`、`上证指数`、`SH000001`、`深证成指`、`创业板指`、`科创50`
- 港股：`00700.HK`
- 纯数字港股：`00700`（5 位数字）
- H 前缀港股：`H00700`
- 中文名称：`腾讯控股`、`贵州茅台`（需 web 数据源）

## 参考资料

按需读取：

- `references/一二三类买卖点详解.md`：一类、二类、三类买卖点的定义和完整逻辑。
- `references/缠中说禅全部名词解析.md`：中枢、走势、背驰、分型、笔、线段、中枢延伸/新生、MACD 证据等辅助概念。

以参考文件中的理论框架作为分析依据。附带的脚本提供机械化的技术证据，最终结论需基于缠论进行推理判断。

## 增强型缠论分析管道

脚本现已实现**完整的缠论分析管道**，包含以下步骤：

1. **K 线包含处理** — 标准缠论包含关系处理（第 62/65 课）
2. **分型识别** — 在包含处理后的 K 线上按三根 K 线形态识别顶底分型，再按分型序列规则过滤；若待过滤分型自身包含反向缺口，则该分型不应被过滤
3. **笔构造** — 严格交替顶底分型，连续同向取极值
4. **线段检测** — 至少 3 笔构成，判断线段方向
5. **中枢定位** — 连续 3 笔重叠区域，支持延伸/扩张/新生
6. **买卖点诊断** — 一二三类买卖点综合评分

## 多级别 K 线标注图

脚本默认生成支持切换的交互式 K 线图，级别包括 1 分钟、5 分钟、30 分钟和日线。默认优先展示 1 分钟；若 1 分钟数据不可用，则自动降级为 5 分钟；若 5 分钟也不可用，再依次降级为 30 分钟、日线，并在页面中显示不可用原因。脚本不在取数阶段人为截断 K 线数量，使用接口可返回的最近全部 K 线。分钟线会多源尝试并选择返回 K 线最多的来源，而不是固定使用单一渠道。

图表使用内联 SVG，支持滚轮缩放、拖拽平移、双击重置和悬停查看 OHLC。缩放按可见 K 线根数设置边界，避免过度放大导致样式失控。

图中标注以下内容：

- **分型**：▲ 底分型 / ▼ 顶分型
- **笔**：连接有效分型端点的向上/向下笔
当前 HTML 报告聚焦 K 线包含处理、顶/底分型和笔构造稳定性，图中不展示线段和中枢。

### 分型过滤中的重合处理

分型序列过滤时，若当前分型与前一个有效分型类型一致，则仍按同类分型取极值：顶分型保留更高者，底分型保留更低者。

当前分型只与前一个有效分型检查占用 K 线和独立 K 线间隔；已过滤的原始分型形态不参与该间隔检查。若当前分型与前一个有效分型共用 K 线，或中间不足 1 根独立 K 线，则优先按分型间隔规则舍弃当前分型，不进入价格区间重叠与同类极值比较。

若当前分型与前一个有效分型类型相反但价格区间重合，则当前分型必须与上一个同类分型比较：

- 前一个有效分型为底分型，当前为顶分型时，当前顶分型的低点不能低于前一底分型的高点；若重合，则比较当前顶分型与上一个顶分型。若上一个顶分型更高，舍弃当前顶分型；若当前顶分型更高，先在上一个有效底分型和上上个有效底分型中舍弃低点更高的那个，再保留当前顶分型。
- 前一个有效分型为顶分型，当前为底分型时，当前底分型的高点不能高于前一顶分型的低点；若重合，则比较当前底分型与上一个底分型。若上一个底分型更低，舍弃当前底分型；若当前底分型更低，先在上一个有效顶分型和上上个有效顶分型中舍弃高点更低的那个，再保留当前底分型。

删除较弱的反向分型后，如果有效序列中出现相邻同类分型，则需要继续向前做同类极值归并：顶分型永远保留高点更高者，底分型永远保留低点更低者；胜出的分型继续与前一个分型比较，直到遇到反向分型或序列起点。

HTML 分型列表最后一列使用“备注”：已过滤分型展示过滤原因；有效分型若因区间重叠、反向弱分型删除或同类极值归并而保留，也应展示保留说明，方便识别可能存在歧义的有效分型。

若两个反向分型的占用 K 线重叠，或中间不足 1 根独立 K 线，则优先按分型间隔规则舍弃当前分型；该情况不再进入价格区间重叠与上一个同类分型比较的逻辑。只有分型占用和独立 K 线间隔满足要求后，才继续判断顶底分型价格区间是否重叠。

### 分型过滤中的反向缺口保护

分型序列过滤时，若原本会被过滤的分型中包括反向缺口，则该分型保留为有效分型：

- 缺口必须基于**未做包含处理的原始相邻 K 线**判断；包含处理后的合并 K 线之间形成的价格断层不算缺口。
- **向下缺口**：前一 K 线最低点高于后一 K 线最高点，且差值超过前一 K 线最低点的 0.6%。
- **向上缺口**：后一 K 线最低点高于前一 K 线最高点，且差值超过前一 K 线最高点的 0.6%。
- **反向缺口**：前一个分型是顶分型，随后 K 线向下跳开形成向下缺口；前一个分型是底分型，随后 K 线向上跳开形成向上缺口。

因此，顶分型右侧 K 线及后一根确认 K 线之间向下跳空超过 0.6%，或底分型右侧 K 线及后一根确认 K 线之间向上跳空超过 0.6% 时，该顶/底分型如果因分型占用区间、独立 K 线不足或区间重叠规则本应过滤，则保留并交由后续笔构造继续判断。若当前待过滤分型占用的三根合并 K 线中，包含“前一有效顶分型之后的向下缺口”或“前一有效底分型之后的向上缺口”，同样按反向缺口保护保留当前分型。但如果当前分型与上一个有效分型类型一致，仍按同类分型过滤规则处理：顶分型取更高者，底分型取更低者，反向缺口不放宽该同类过滤。

## 工作流程

1. **解析股票**：
   - 接受股票代码或股票名称。
   - 始终使用网络来源搜索并获取行情数据。
   - 不读取本地 CSV 缓存，除非用户明确要求调试本地数据。
   - 如果名称无法通过网络唯一匹配，则询问用户股票代码。

2. **运行分析脚本**：

```bash
# 使用网络来源并生成多级别切换报告（默认优先 1 分钟）：
python3 /Users/josan/.workbuddy/skills/chan-stock-buy-sell-point/scripts/analyze_chan_points.py \
  --stock "<股票名称或代码>" \
  --source web \
  --out-dir /Users/josan/Desktop/czsc/reports \
  --chart-timeframe auto

# 指定股票名称（名称自动解析）：
python3 /Users/josan/.workbuddy/skills/chan-stock-buy-sell-point/scripts/analyze_chan_points.py \
  --stock "腾讯控股" \
  --source web \
  --out-dir /Users/josan/Desktop/czsc/reports

# 若网络分钟线不稳定，可只生成网络日线报告：
python3 /Users/josan/.workbuddy/skills/chan-stock-buy-sell-point/scripts/analyze_chan_points.py \
  --stock "000001" \
  --source web \
  --out-dir /Users/josan/Desktop/czsc/reports \
  --chart-timeframe daily
```

3. **读取脚本输出**：从命令行输出中提取生成的 JSON 摘要和 HTML 路径。

4. **生成中文最终回复**，包含：
   - 当前结论：是/否为一类买卖点、二类买卖点、三类买卖点、候选点，或没有确认的买卖点。
   - 按类目列出证据：趋势/中枢位置、背驰、一买后的首次回踩、三买的中枢回试、均线、MACD。
   - 反证与失效条件。
   - 数据范围、数据来源和分析级别。
   - 生成的 HTML 报告链接/路径。

## 报告逻辑

脚本使用日线 OHLCV 数据构建实用的缠论近似分析：

- **分型**：确认的局部顶/底转折点。
- **笔/线段**：交替的转折点，经过最小距离和价格变动过滤。
- **中枢**：连续三笔的重叠区域。中枢区间为 `[ZD, ZG]`；`ZD` 为下沿，`ZG` 为上沿。
- **MACD**：EMA(12)、EMA(26)、DEA(9) 及柱状图面积，用于背驰判断。
- **均线**：MA5/MA10/MA20/MA60，用于研判男上位/女上位及趋势背景。

信号分类遵循保守原则：

- **一类买点**：前一段下跌趋势或中枢向下移动，当前/近期低点低于最近中枢，且下行力量减弱或 MACD 底背驰。
- **一类卖点**：前一段上涨趋势或中枢向上移动，当前/近期高点高于最近中枢，且上行力量减弱或 MACD 顶背驰。
- **二类买点**：在一类买点候选后，首次回踩的低点守住或接近一买低点，且未破坏反弹结构。
- **二类卖点**：在一类卖点候选后，首次反弹的高点未能突破或接近一卖高点，且未破坏下跌结构。
- **三类买点**：价格向上离开某个中枢后，首次回踩的低点未跌回该中枢的 `ZG` 以内。
- **三类卖点**：价格向下离开某个中枢后，首次反弹的高点未回升到该中枢的 `ZD` 以上。

当多个标签同时适用时，需解释其间关系。二类买点可与三类买点重叠；一类买点与本级别上的二类/三类买点不重叠。

## 输出结构

在对话回复中使用以下结构：

```markdown
**结论**
<一句话结论，含买卖点类型和置信度>

**关键证据**
- <证据 1>
- <证据 2>
- <证据 3>

**反证与风险**
- <反证或失效条件>
- <数据/级别限制>

**报告**
HTML 报告：<绝对路径链接>
```

HTML 报告当前用于基础标注稳定性测试，应聚焦 K 线包含处理、顶/底分型和笔构造：图中展示 K 线、顶分型、底分型和笔；正文展示包含处理后 K 线列表、原始分型列表和笔列表。不要在 HTML 中展示线段、中枢或买卖点证据表，等基础分型与笔稳定后再恢复后续结构。

## 分析纪律

- 始终区分"确认"与"候选"。当前日线数据通常只能支持"候选/近似确认"，因为缺乏更低级别的确认。
- 提及数据来源和分析级别。默认使用网络数据，需注明实际成功渠道或报告中的来源标签，例如 `AKShare-新浪财经`、`东方财富`。如果仅有日线数据，则说明这是"日线级别近似分析"；真正的缠论确认往往需要更低级别（如 30m/5m）进行区间套验证。
- 使用具体价格水平定义失效条件：最新中枢的 `ZG/ZD`、近期转折高低点、一买/一卖的候选低点/高点。
- 不使用绝对化的盈利承诺。将理论中的绝对断言替换为实际措辞，如"按该级别结构，后续应重点观察……"。
- 如果数据陈旧，需在报告和最终回复中注明最新的 `trade_date`。

## 示例

输入：

```text
分析 600966.SH 当前是不是一二三类买卖点
```

输出：

```text
日线级别当前更接近三类买点候选：价格向上离开最近中枢后首次回试，回试低点暂未跌破 ZG；但 MACD 未形成强同步确认，因此标记为候选而非确认。HTML 报告已生成：...
```

输入：

```text
帮我看一下贵州茅台是不是一买
```

使用网络来源：

```text
已通过网络获取贵州茅台行情，日线级别当前...
```

输入：

```text
分析腾讯控股是不是二三买
```

使用网络来源：

```text
通过网络获取腾讯控股(00700.HK)日线数据，日线级别当前...
```
